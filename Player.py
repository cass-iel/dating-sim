class Player:
    def __init__(self, saved: bool):
        if saved is True:
            # Runs if there is a save file to load from
            pass
        else:
            # Runs if this is a new game
            pass
